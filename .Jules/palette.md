## 2025-02-18 - Missing Global Accessibility Structure
**Learning:** Found multiple pages duplicating global UI elements (cursor) and missing core accessibility features (skip link).
**Action:** Centralize global UI elements in `layout.tsx` and ensure `main` content is always targetable via ID.
